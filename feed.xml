<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://gmichelcarvalho.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://gmichelcarvalho.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-27T23:40:56+00:00</updated><id>https://gmichelcarvalho.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal site of Michel Carvalho — ML researcher and engineer. Writing about machine learning, deep learning, and related topics. </subtitle><entry><title type="html">The Bias–Variance Tradeoff, Carefully</title><link href="https://gmichelcarvalho.github.io/blog/2026/bias-variance-tradeoff/" rel="alternate" type="text/html" title="The Bias–Variance Tradeoff, Carefully"/><published>2026-05-20T12:00:00+00:00</published><updated>2026-05-20T12:00:00+00:00</updated><id>https://gmichelcarvalho.github.io/blog/2026/bias-variance-tradeoff</id><content type="html" xml:base="https://gmichelcarvalho.github.io/blog/2026/bias-variance-tradeoff/"><![CDATA[<p>The bias–variance tradeoff is one of the oldest ideas in statistics, but it is also one of the most frequently misunderstood — especially in light of modern deep learning, where highly overparameterized models generalize well despite having near-zero training loss.</p> <h2 id="the-decomposition">The Decomposition</h2> <p>Let \(y = f(x) + \epsilon\) where \(\epsilon \sim \mathcal{N}(0, \sigma^2)\) is irreducible noise. Given an estimator \(\hat{f}\) learned from a dataset \(\mathcal{D}\), the expected squared error at a point \(x\) decomposes as:</p> \[\mathbb{E}_{\mathcal{D},\epsilon}\!\left[(y - \hat{f}(x))^2\right] = \underbrace{\left(\mathbb{E}_{\mathcal{D}}[\hat{f}(x)] - f(x)\right)^2}_{\text{Bias}^2} + \underbrace{\mathbb{E}_{\mathcal{D}}\!\left[\left(\hat{f}(x) - \mathbb{E}_{\mathcal{D}}[\hat{f}(x)]\right)^2\right]}_{\text{Variance}} + \underbrace{\sigma^2}_{\text{Noise}}\] <p><strong>Proof sketch.</strong> Let \(\bar{f} = \mathbb{E}_\mathcal{D}[\hat{f}(x)]\). Expand \((y - \hat{f})^2 = (y - \bar{f} + \bar{f} - \hat{f})^2\) and use the fact that the cross term vanishes in expectation because \(\epsilon\) is independent of \(\mathcal{D}\), and \(\hat{f} - \bar{f}\) is mean-zero over \(\mathcal{D}\).</p> <h2 id="the-classical-picture">The Classical Picture</h2> <p>In the classical view, model complexity controls a tradeoff:</p> <ul> <li><strong>High bias, low variance</strong>: simple model (e.g., linear regression on nonlinear data). Consistently wrong but in a predictable way.</li> <li><strong>Low bias, high variance</strong>: complex model (e.g., degree-100 polynomial). Gets training data right but fluctuates wildly with different samples.</li> </ul> <p>This produces the familiar U-shaped test error curve as a function of model complexity.</p> <h2 id="the-double-descent-phenomenon">The Double Descent Phenomenon</h2> <p>Modern neural networks break this picture. As model size grows past the interpolation threshold (where the model can fit the training data exactly), test error often <em>decreases again</em> — a phenomenon called <strong>double descent</strong> (Belkin et al., 2019).</p> <p>The classical U-curve is just the left portion. After the interpolation peak, the model has so many parameters that among all solutions that fit the training data, implicit regularization (e.g., from SGD or the inductive bias of the architecture) selects a smooth, low-variance one.</p> <p>This suggests the decomposition is still valid, but the mapping from “model size” to “bias” and “variance” is more nuanced than the classical picture suggests: larger models can simultaneously achieve low bias <em>and</em> lower variance than slightly smaller models.</p> <h2 id="practical-takeaways">Practical takeaways</h2> <ol> <li>The decomposition is exact — it is not an approximation or a heuristic.</li> <li>Variance is reducible by ensembling or averaging over random seeds.</li> <li>Bias is reducible by using a richer model class.</li> <li>Neither tells you about computational cost or the quality of your optimization.</li> </ol> <h2 id="further-reading">Further reading</h2> <ul> <li>Belkin et al. (2019), <em>Reconciling modern machine learning and the bias–variance tradeoff</em>: <a href="https://arxiv.org/abs/1812.11118">arxiv.org/abs/1812.11118</a></li> <li>Hastie, Tibshirani &amp; Friedman, <em>The Elements of Statistical Learning</em> (free PDF) — Chapter 7 for the classical treatment.</li> </ul>]]></content><author><name></name></author><category term="math"/><category term="tutorial"/><category term="deep-learning"/><summary type="html"><![CDATA[A rigorous derivation of the bias-variance decomposition and what it actually tells us about modern overparameterized models.]]></summary></entry><entry><title type="html">Diffusion Models from First Principles</title><link href="https://gmichelcarvalho.github.io/blog/2026/diffusion-models-intro/" rel="alternate" type="text/html" title="Diffusion Models from First Principles"/><published>2026-05-10T12:00:00+00:00</published><updated>2026-05-10T12:00:00+00:00</updated><id>https://gmichelcarvalho.github.io/blog/2026/diffusion-models-intro</id><content type="html" xml:base="https://gmichelcarvalho.github.io/blog/2026/diffusion-models-intro/"><![CDATA[<p>Diffusion models have become the dominant paradigm for image (and now audio, video, and 3D) generation. This post builds up the theory from scratch.</p> <h2 id="the-forward-process">The Forward Process</h2> <p>The forward process gradually corrupts data \(x_0 \sim q(x_0)\) by adding Gaussian noise across \(T\) steps:</p> \[q(x_t \mid x_{t-1}) = \mathcal{N}\!\left(x_t;\; \sqrt{1-\beta_t}\, x_{t-1},\; \beta_t I\right)\] <p>where \(\{\beta_t\}_{t=1}^T\) is a fixed variance schedule. A crucial property is that we can sample \(x_t\) at any step directly from \(x_0\) in closed form. Define \(\alpha_t = 1 - \beta_t\) and \(\bar{\alpha}_t = \prod_{s=1}^t \alpha_s\). Then:</p> \[q(x_t \mid x_0) = \mathcal{N}\!\left(x_t;\; \sqrt{\bar{\alpha}_t}\, x_0,\; (1 - \bar{\alpha}_t) I\right)\] <p>Or equivalently: \(x_t = \sqrt{\bar{\alpha}_t}\, x_0 + \sqrt{1 - \bar{\alpha}_t}\, \epsilon\), where \(\epsilon \sim \mathcal{N}(0, I)\).</p> <p>As \(t \to T\), \(\bar{\alpha}_t \to 0\), so \(x_T \approx \mathcal{N}(0, I)\) — pure noise.</p> <h2 id="the-reverse-process">The Reverse Process</h2> <p>The reverse process \(p_\theta(x_{t-1} \mid x_t)\) is what we learn. The posterior \(q(x_{t-1} \mid x_t, x_0)\) is tractable (since conditioning on \(x_0\) makes everything Gaussian):</p> \[q(x_{t-1} \mid x_t, x_0) = \mathcal{N}(x_{t-1};\; \tilde{\mu}_t(x_t, x_0),\; \tilde{\beta}_t I)\] <p>where the mean \(\tilde{\mu}_t\) and variance \(\tilde{\beta}_t\) have closed-form expressions in terms of \(\alpha_t\) and \(\bar{\alpha}_t\).</p> <p>The idea of DDPM (Ho et al., 2020): train a neural network \(\epsilon_\theta(x_t, t)\) to predict the noise \(\epsilon\) added to \(x_0\). The simplified training objective is:</p> \[\mathcal{L}_{\text{simple}} = \mathbb{E}_{t, x_0, \epsilon}\!\left[\|\epsilon - \epsilon_\theta(x_t, t)\|^2\right]\] <h2 id="score-matching-connection">Score Matching Connection</h2> <p>The noise-prediction network is closely related to the <strong>score function</strong> \(\nabla_{x_t} \log q(x_t)\). Since:</p> \[\nabla_{x_t} \log q(x_t \mid x_0) = -\frac{\epsilon}{\sqrt{1 - \bar{\alpha}_t}}\] <p>learning \(\epsilon_\theta\) is equivalent to learning a scaled score. This connection to score-based generative models (Song &amp; Ermon, 2019) unifies several lines of work under a common probabilistic framework.</p> <h2 id="sampling">Sampling</h2> <p>Generation proceeds by iterating the learned reverse step from \(x_T \sim \mathcal{N}(0, I)\) down to \(x_0\). DDPM uses \(T = 1000\) steps, making generation slow. DDIM (Song et al., 2020) introduces a deterministic sampler that works with far fewer steps (e.g., 50) by reinterpreting the forward process as a non-Markovian one.</p> <h2 id="further-reading">Further reading</h2> <ul> <li>DDPM: <a href="https://arxiv.org/abs/2006.11239">arxiv.org/abs/2006.11239</a></li> <li>Score-based models: <a href="https://arxiv.org/abs/1907.05600">arxiv.org/abs/1907.05600</a></li> <li>Lilian Weng’s excellent overview: <a href="https://lilianweng.github.io/posts/2021-07-11-diffusion-models/">lilianweng.github.io/posts/2021-07-11-diffusion-models</a></li> </ul>]]></content><author><name></name></author><category term="deep-learning"/><category term="tutorial"/><category term="math"/><summary type="html"><![CDATA[Understanding score-based generative models and DDPMs through the math, not just the intuition.]]></summary></entry><entry><title type="html">Attention Is All You Need — a close reading</title><link href="https://gmichelcarvalho.github.io/blog/2026/attention-mechanism/" rel="alternate" type="text/html" title="Attention Is All You Need — a close reading"/><published>2026-05-01T12:00:00+00:00</published><updated>2026-05-01T12:00:00+00:00</updated><id>https://gmichelcarvalho.github.io/blog/2026/attention-mechanism</id><content type="html" xml:base="https://gmichelcarvalho.github.io/blog/2026/attention-mechanism/"><![CDATA[<p>The 2017 paper <em>Attention Is All You Need</em> (Vaswani et al.) replaced recurrence with self-attention and became the backbone of nearly every modern NLP system. This post walks through the key ideas carefully, with derivations.</p> <h2 id="scaled-dot-product-attention">Scaled Dot-Product Attention</h2> <p>Given queries \(Q \in \mathbb{R}^{n \times d_k}\), keys \(K \in \mathbb{R}^{m \times d_k}\), and values \(V \in \mathbb{R}^{m \times d_v}\), the attention function is:</p> \[\text{Attention}(Q, K, V) = \text{softmax}\!\left(\frac{QK^\top}{\sqrt{d_k}}\right)V\] <p>The \(\sqrt{d_k}\) scaling factor is often glossed over, but it matters: without it, dot products grow large in magnitude as \(d_k\) grows, pushing the softmax into regions with very small gradients.</p> <h3 id="why-dot-products-grow">Why dot products grow</h3> <p>If \(q\) and \(k\) are independent random vectors with zero mean and unit variance components, then:</p> \[q \cdot k = \sum_{i=1}^{d_k} q_i k_i \implies \mathbb{E}[q \cdot k] = 0, \quad \text{Var}[q \cdot k] = d_k\] <p>So the standard deviation scales as \(\sqrt{d_k}\). Dividing by \(\sqrt{d_k}\) restores unit variance, keeping softmax in a well-behaved regime.</p> <h2 id="multi-head-attention">Multi-Head Attention</h2> <p>Instead of computing a single attention function, the paper projects \(Q, K, V\) into \(h\) lower-dimensional subspaces, computes attention in each, and concatenates:</p> \[\text{MultiHead}(Q,K,V) = \text{Concat}(\text{head}_1, \ldots, \text{head}_h)\, W^O\] <p>where \(\text{head}_i = \text{Attention}(QW_i^Q, KW_i^K, VW_i^V)\).</p> <p>This lets the model attend to information from different representation subspaces simultaneously — something a single wide attention head cannot do as effectively.</p> <h2 id="positional-encoding">Positional Encoding</h2> <p>Self-attention is permutation-equivariant: if you shuffle the tokens, the outputs shuffle identically. To inject sequence order, the paper adds sinusoidal positional encodings to the token embeddings:</p> \[PE_{(pos, 2i)} = \sin\!\left(\frac{pos}{10000^{2i/d}}\right), \quad PE_{(pos, 2i+1)} = \cos\!\left(\frac{pos}{10000^{2i/d}}\right)\] <p>The key property is that for any fixed offset \(k\), \(PE_{pos+k}\) is a linear function of \(PE_{pos}\), making it easy for the model to reason about relative positions.</p> <h2 id="what-to-read-next">What to read next</h2> <ul> <li>The original paper: <a href="https://arxiv.org/abs/1706.03762">arxiv.org/abs/1706.03762</a></li> <li>Jay Alammar’s illustrated transformer for visual intuition</li> <li><em>Flash Attention</em> (Dao et al., 2022) for the IO-efficient implementation that makes modern LLM training practical</li> </ul>]]></content><author><name></name></author><category term="deep-learning"/><category term="nlp"/><category term="tutorial"/><summary type="html"><![CDATA[A careful walk-through of the original Transformer paper, with derivations and intuitions for the attention mechanism.]]></summary></entry></feed>